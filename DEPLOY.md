# Vercel deployment

Upload the contents of this folder directly as the Vercel project root.

Root Directory: `./`
Framework: Other
Build Command: empty
Output Directory: empty
Install Command: empty

After deployment, hard refresh the browser (`Ctrl+Shift+R`) to clear an older cached app script.
